# Frontend Bootstrap

- Gulp for builds
- Bower for JS/CSS libraries
- JavaScript
  - ES6 (via Babel)
  - Browserify
  - Incremental builds (via Watchify)
  - `window.fetch` polyfill for AJAX
- CSS
  - cssnext
  - Autoprefixer
  - Normalize.css
